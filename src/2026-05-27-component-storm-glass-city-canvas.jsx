// =============================================================================
// StormGlassCityCanvas — wallpaper I (stub)
// Placeholder: original was a markdown README, not a working component, which
// blocked the entire app from loading. This stub renders nothing so the design
// 'I' slot still cycles in the picker (its CSS background gradient from
// DESIGNS.I provides the look until the real canvas engine is implemented).
// =============================================================================
import React from 'react';

export default function StormGlassCityCanvas() {
  return null;
}
