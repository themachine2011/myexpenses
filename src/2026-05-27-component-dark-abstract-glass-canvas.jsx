// =============================================================================
// DarkAbstractGlassCanvas — wallpaper K (stub)
// Placeholder: file was never created but glass-theme.jsx imports + renders it
// for design 'K', which broke the entire app load. This stub renders nothing
// so the design 'K' slot still cycles in the picker (its CSS background
// gradient from DESIGNS.K provides the look until the real canvas engine is
// implemented).
// =============================================================================
import React from 'react';

export default function DarkAbstractGlassCanvas() {
  return null;
}
